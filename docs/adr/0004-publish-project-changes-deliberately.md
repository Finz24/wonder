# Publish Project changes deliberately

The Creator prepares Project content as a Draft, inspects it in Preview, deliberately makes it Published, and may later make it Archived. Editing must not silently replace the public presentation; this adds lifecycle complexity so the Creator can work independently without exposing unfinished changes.
