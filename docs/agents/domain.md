# Domain docs

This is a single-context repository.

Before exploring, read `CONTEXT.md` and any relevant ADRs under `docs/adr/`. Use the glossary vocabulary from `CONTEXT.md` in issues, specs, tests, and implementation discussions. If an output conflicts with an ADR, surface the conflict explicitly.

The domain layout is:

```
CONTEXT.md
docs/adr/
```
